{% extends 'layouts/content.html.twig' %}

{% block main %}
  <div class="container p-4">
    <h1 class="h3">{{ 'DeChess'|trans }}</h1>
    <hr class="hr">
    <form id="dechessForm">
      <div>
        <div class="mb-3">
          <label class="form-label">{{ 'Chess Server'|trans }}</label>
          <select name="locale" class="form-select" required>
            <option value="">{{ 'Select an option'|trans }}</option>
          </select>
        </div>
        <div class="mb-3">
          <label class="form-label">{{ 'Chess API'|trans }}</label>
          <select name="theme" class="form-select" required>
            <option value="">{{ 'Select an option'|trans }}</option>
          </select>
        </div>
              <div>
          <button type="submit" class="btn btn-primary">{{ 'Save Changes'|trans }}</button>
        </div>
      </div>
    </form>
  </div>
  {% block javascripts %}
    {% block importmap %}
      {{ importmap('js/pages/dechess/index.js') }}
    {% endblock %}
  {% endblock %}
{% endblock %}
